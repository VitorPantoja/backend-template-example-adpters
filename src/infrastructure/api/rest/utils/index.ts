import Environment from './helper/environment';
import { ExecutionTime } from './helper/execution-time';
import { TerminalLogger } from './helper/terminal-logger';

export const Utils = {
  Environment,
  TerminalLogger,
  ExecutionTime,
};
